{
  "name": "stripe-webhook",
  "version": "1.0.0",
  "type": "module",
  "description": "Stripe webhook receiver with signature verification, running on wawesome.io",
  "license": "MIT",
  "scripts": {
    "build": "wawesome build",
    "deploy": "wawesome deploy",
    "test": "vitest run",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "stripe": "^22.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wawesome": "^0.0.9"
  }
}
